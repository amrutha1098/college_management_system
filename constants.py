# to do  >>  Use logging module instead of print for different levels such as debug, error and info
# to do >> Replace the DB constants into environment variables and value should be loaded from .env files
# to do >> custom create a response code / status for db query which fails
DB_USER = '*********'
DB_HOST = '*********'
DB_PASSWORD = '**********'
DB_DATABASE = 'CMS'